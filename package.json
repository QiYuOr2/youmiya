{
  "private": true,
  "scripts": {
    "dev:ssg": "pnpm --filter @ymy/ssg dev",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:cli",
    "eslint": "catalog:cli",
    "fs-extra": "catalog:utils",
    "lint-staged": "catalog:cli",
    "simple-git-hooks": "catalog:cli",
    "yaml": "catalog:utils"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
