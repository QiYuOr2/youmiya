{
  "private": true,
  "scripts": {
    "build": "pnpm --filter @ymy/ssg build",
    "dev:ssg": "pnpm --filter @ymy/ssg dev",
    "lint": "eslint .",
    "spider": "pnpm --filter @ymy/spider start"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:cli",
    "eslint": "catalog:cli",
    "fs-extra": "catalog:utils",
    "lint-staged": "catalog:cli",
    "simple-git-hooks": "catalog:cli",
    "yaml": "catalog:parser"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
